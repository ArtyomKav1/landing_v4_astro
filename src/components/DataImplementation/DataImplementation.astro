---
import { data } from "./DataImplementation.constants";

---

<div
    class="px-12 max-sm:px-5 py-[88px] flex flex-col max-w-[1224px] mx-auto"
>
    <div
        class="text-center text-[#0D0628] text-[26px] max-sm:text-[22px] max-sm:leading-7 font-semibold leading-[34px] pb-10"
    >
        Внедрение данных в любые корпоративные системы
    </div>
    <div class="flex flex-wrap gap-2 justify-center max-w-[936px] mx-auto">
        {
            data.map((item) => (
                <div class="bg-[#FFFFFF] border border-[#8C26EA] px-5 py-3 rounded-2xl text-[16px] hover:-translate-y-1 transition-all duration-300">
                    {item}
                </div>
            ))
        }
    </div>
</div>
